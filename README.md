# Mo Bot 🟣

- ترحيب الأعضاء الجدد برسالة وصورة.  
- نظام تذاكر مع مكافآت نقاط.  
- نظام نقاط للـ Chat والـ Voice.  
- رتب خاصة يمكن للمالك إضافة حتى شخصين إضافيين لكل رتبة.  
- لوحة تحكم لإدارة الرتب الخاصة.  

---

## ⚡ المتطلبات

- Node.js >= 18  
- Discord.js v14  
- حساب Discord Bot مع صلاحيات كافية (إدارة الرتب، إرسال رسائل، إدارة القنوات)  

---

## 📦 التثبيت

1. استنسخ المشروع:

\`\`\`bash
git clone https://github.com/M-ahde/Mo-Bot.git
cd Mo-Bot
\`\`\`

2. ثبّت الحزم:

\`\`\`bash
npm install
\`\`\`

---

## 🌐 إعداد ملف .env

البوت يحتاج **ملف .env** لتشغيله بشكل صحيح.  
قم بإنشاء ملف `.env` في نفس مجلد `index.js` وضع فيه القيم التالية:

\`\`\`env
TOKEN=YOUR_BOT_TOKEN
CLIENT_ID=YOUR_CLIENT_ID
GUILD_ID=YOUR_GUILD_ID
\`\`\`

- `TOKEN` → توكن البوت الخاص بك.  
- `CLIENT_ID` → معرف البوت (Application ID).  
- `GUILD_ID` → معرف السيرفر الخاص بك.  

> ⚠️ **ملاحظة:** هذا الملف **مهم جداً** لعمل البوت، ولا تشاركه مع أي شخص.

---

## 🛠 إعداد ملف Config

**config.js** يحتوي على جميع الإعدادات الخاصة بالبوت:

\`\`\`js
export default {
    welcome: {
        message: \`أهلاً بك في السيرفر، <@{user}>! 🎉\`,
        image: "https://raw.githubusercontent.com/ZarScape/ZarScape/refs/heads/main/images/ZarScape/logo-with-background.png",
        channelId: "WELCOME_CHANNEL_ID"
    },

    ticket: {
        categories: ["TICKET_CATEGORY_ID"], 
        allowedRoles: ["ALLOWED_ROLE_ID"], 
        rewardPoints: 50,
    },

    points: {
        chatCooldown: 1, 
        voiceCooldown: 0, 
        chatPoints: 5,
        voicePoints: 10,
        maxMessageLength: 2000,
        log: "POINTS_LOG_CHANNEL_ID"
    },

    specialRole: {
        positionBelowRoleId: "BASE_ROLE_ID", 
        color: "Blue", 
    },

    rolePermissionsOptions: [
        { label: "إرسال رسائل", value: "SendMessages" },
        { label: "إرسال صور وملفات", value: "AttachFiles" },
        { label: "وضع ردود أفعال", value: "AddReactions" },
        { label: "استخدام إيموجيات خارجية", value: "UseExternalEmojis" },
        { label: "إرسال ستكرات", value: "SendMessagesInThreads" }
    ]
};
\`\`\`

> ⚠️ تأكد من وضع **الـ IDs الصحيحة** لكل قناة أو رتبة في سيرفرك.

---

## �� تشغيل البوت

\`\`\`bash
npm start
\`\`\`

سيقوم البوت بالآتي:

- تسجيل النقاط تلقائيًا عند التفاعل بالـ Chat والـ Voice.  
- إرسال رسالة الترحيب عند دخول أي عضو جديد.  
- إرسال زر استلام التذاكر داخل التذاكر الجديدة.  
- تحديث وإدارة الرتب الخاصة عبر لوحة التحكم.

---

## 💳 أوامر البوت الرئيسية

### النقاط

- \`/points [@user]\` → عرض نقاط العضو أو نفسك.  
- \`/addpoints @user <عدد>\` → إضافة نقاط (للأدارة فقط).  
- \`/removepoints @user <عدد>\` → إزالة نقاط (للأدارة فقط).  
- \`/setpoints @user <عدد>\` → تحديد عدد النقاط مباشرة (للأدارة فقط).  

### الرتب الخاصة

- \`/special-dashboard\` → إرسال لوحة التحكم بالرتب الخاصة (للأدارة فقط).  
- من لوحة التحكم يمكن:  
  - تغيير اسم الرتبة  
  - تغيير لون الرتبة  
  - تغيير أيقونة الرتبة  
  - إضافة أو إزالة مستخدمين للرتبة (حتى شخصين إضافيين لكل رتبة)  

---

## ⏰ الحذف التلقائي للرتب الخاصة

- الرتب الخاصة التي تحتوي على مدة (\`expiresAt\`) يتم فحصها تلقائيًا كل 6 ساعات.  
- عند انتهاء مدة الرتبة، يتم:  
  - إزالة الرتبة من المالك والمستخدمين الآخرين  
  - حذف الرتبة من قاعدة البيانات  
  - إرسال رسالة خاصة للمالك لإعلامه بانتهاء الرتبة  

---

## ⚡ ملاحظات

- البوت يحتاج صلاحيات **إدارة الرتب** لإضافة/إزالة الرتب والمستخدمين.  
- للتأكد من عمل النقاط، يجب أن يكون لدى البوت صلاحية **قراءة القنوات وإرسال الرسائل**.  
- يمكن تعديل الإعدادات في \`config.js\` بما يناسب سيرفرك.
